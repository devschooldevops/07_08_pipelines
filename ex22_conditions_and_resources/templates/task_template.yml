parameters:
  environment_task: "env"

steps:
- task: Bash@3
  name: CheckParam 
  displayName: 'Check parameters for ${{ parameters.environment_task }}' 
  inputs:
    targetType: 'inline'
    script: |
      echo "My name is ..."
      echo "This is a job running on ${{ parameters.environment_task }}"