variables:
- name: devschool
  value: initialValue 
steps:
  - script: |
      echo ${{ variables.devschool }}
      echo $(devschool)
    displayName: First variable pass
  - script: echo "##vso[task.setvariable variable=one]secondValue"
    displayName: Set new variable value
  - script: |
      echo ${{ variables.devschool}} 
      echo $(devschool)
    displayName: Second variable pass

